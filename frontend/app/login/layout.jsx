export const metadata = {
  title: "Login",
  description: "Securely log in to your Sahu Sabha account to connect with the community and access exclusive services.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
