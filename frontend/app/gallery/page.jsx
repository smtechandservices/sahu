import GalleryPage from '../../components/Gallery/GalleryPage';

export const metadata = {
  title: 'Photo Gallery',
  description: 'Explore our community moments — events, celebrations, and memories captured by Sahu Sabha.',
};

export default function Gallery() {
  return <GalleryPage />;
}
