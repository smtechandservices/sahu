import EventsClient from '../../components/Events/EventsClient';

export const metadata = {
  title: 'Events Calendar | Sahu Sabha',
  description: 'Stay updated with upcoming community events, festivals, and networking mixers organized by Sahu Sabha.',
};

export default function EventsPage() {
  return <EventsClient />;
}
