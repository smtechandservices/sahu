export const metadata = {
  title: "Join Community",
  description: "Register to join the Sahu Sabha community. Connect with families, access exclusive digital magazines, and support our heritage.",
};

export default function JoinLayout({ children }) {
  return <>{children}</>;
}
